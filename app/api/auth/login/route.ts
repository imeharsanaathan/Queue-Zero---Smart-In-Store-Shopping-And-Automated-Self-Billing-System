import {NextResponse} from 'next/server'; import {createSession,createUser,loginUser} from '@/lib/db';
export async function POST(req:Request){const body=await req.json().catch(()=>({}));try{const user=body.mode==='register'?await createUser({name:String(body.name||''),email:String(body.email||''),phone:String(body.phone||''),password:String(body.password||'')}):await loginUser({email:String(body.email||''),phone:String(body.phone||''),password:String(body.password||'')});const token=await createSession(user.id);const res=NextResponse.json({ok:true,user});res.cookies.set('qz_session',token,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:30*86400});res.cookies.delete('qz_city');res.cookies.delete('qz_store');return res;}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'Authentication failed.'},{status:409});}}
