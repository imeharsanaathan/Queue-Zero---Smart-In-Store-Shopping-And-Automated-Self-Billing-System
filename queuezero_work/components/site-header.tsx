'use client';
import {Search,MapPin,ShoppingCart,UserRound,LogOut,PackageCheck} from 'lucide-react';
import {BrandMark} from './brand-mark'; import {Button} from './ui/button'; import {useQueueStore} from '@/lib/store'; import {useRouter} from 'next/navigation'; import {toast} from 'sonner';
export function SiteHeader({user,onCart}:{user:{name:string;email:string;phone:string};onCart:()=>void}){const router=useRouter();const{city,store,setSearch,search}=useQueueStore();async function logout(){await fetch('/api/auth/logout',{method:'POST'});toast.success('Signed out securely');try{localStorage.removeItem('queuezero-session')}catch{};window.location.assign('/login')}return <header className="sticky top-0 z-40 border-b border-slate-200 bg-white shadow-[0_2px_12px_rgba(15,23,42,.06)]"><div className="mx-auto max-w-[1560px] px-3 sm:px-5"><div className="flex items-center gap-3 py-2.5"><BrandMark compact/><div className="hidden min-w-[280px] items-center gap-2 rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 md:flex"><MapPin className="h-5 w-5 text-emerald-600"/><div className="min-w-0"><div className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Shopping at</div><div className="truncate text-sm font-black">{store?.name}</div></div></div><div className="relative flex-1"><Search className="absolute left-3.5 top-1/2 h-4.5 w-4.5 -translate-y-1/2 text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search products, brands and more..." className="h-11 w-full rounded-xl border border-slate-200 bg-slate-50 pl-10 pr-4 text-sm font-medium outline-none transition focus:border-emerald-400 focus:bg-white focus:ring-4 focus:ring-emerald-500/10"/></div><div className="flex items-center gap-1"><div className="relative hidden sm:block"><details className="group"><summary className="flex cursor-pointer list-none items-center gap-2 rounded-xl px-3 py-2 text-sm font-bold hover:bg-slate-50"><UserRound className="h-4 w-4"/>Hi, {user.name.split(' ')[0]}</summary><div className="absolute right-0 top-11 w-64 rounded-2xl border border-slate-200 bg-white p-3 shadow-2xl"><div className="rounded-xl bg-slate-50 p-3"><div className="text-[10px] font-bold text-slate-400">SIGNED IN AS</div><div className="mt-1 font-black">{user.name}</div><div className="mt-1 truncate text-xs text-slate-500">{user.email||user.phone}</div></div><button className="mt-2 flex w-full items-center gap-2 rounded-xl px-3 py-2.5 text-sm font-bold hover:bg-slate-50"><PackageCheck className="h-4 w-4 text-blue-600"/>My orders</button><button onClick={logout} className="flex w-full items-center gap-2 rounded-xl px-3 py-2.5 text-sm font-bold text-rose-600 hover:bg-rose-50"><LogOut className="h-4 w-4"/>Sign out</button></div></details></div><Button size="sm" variant="success" onClick={onCart}><ShoppingCart className="h-4 w-4"/>Cart<span className="grid min-w-5 place-items-center rounded-full bg-white px-1 text-[10px] text-emerald-700">{useQueueStore(s=>s.cart.reduce((a,x)=>a+x.qty,0))}</span></Button></div></div><div className="flex items-center gap-2 overflow-x-auto border-t border-slate-100 py-2 no-scrollbar">{['All Categories','Best Sellers','Groceries','Fashion','Electronics','Home & Kitchen','Beauty','Toys & More'].map((x,i)=><button key={x} onClick={()=>{if(i===0)document.getElementById('shop')?.scrollIntoView({behavior:'smooth'});if(i===1)document.getElementById('shop')?.scrollIntoView({behavior:'smooth'});}} className={`whitespace-nowrap rounded-full px-3 py-1.5 text-xs font-extrabold ${i===0?'bg-blue-600 text-white':'text-slate-600 hover:bg-slate-100'}`}>{x}</button>)}<div className="ml-auto hidden shrink-0 text-[11px] font-bold text-emerald-700 sm:block">{city?.name} • {store?.brand} • {store?.kind}</div><button onClick={()=>router.push('/city')} className="shrink-0 rounded-full px-3 py-1.5 text-xs font-black text-blue-600 hover:bg-blue-50">Change store</button></div></div></header>}
