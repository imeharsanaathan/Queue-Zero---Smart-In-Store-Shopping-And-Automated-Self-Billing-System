'use client';
import {X, TrendingDown, Store, ArrowDownRight} from 'lucide-react'; import {Product} from '@/lib/data'; import {money} from '@/lib/utils'; import {Button} from './ui/button'; import {Badge} from './ui/badge';
export function CompareDrawer({product,onClose}:{product:Product|null;onClose:()=>void}){if(!product)return null; const entries=Object.entries(product.compare).sort((a,b)=>a[1]-b[1]); return <div className="fixed inset-0 z-[70] bg-slate-950/35 backdrop-blur-sm" onClick={onClose}><div onClick={e=>e.stopPropagation()} className="absolute bottom-0 right-0 top-0 w-full max-w-[500px] overflow-y-auto bg-white p-4 shadow-2xl sm:p-6"><div className="flex items-center justify-between"><div><Badge tone="green"><TrendingDown className="mr-1 h-3 w-3"/>Live price intelligence</Badge><h3 className="mt-3 text-2xl font-black">Compare {product.brand}</h3><p className="mt-1 text-sm text-slate-500">{product.name}</p></div><button onClick={onClose} className="rounded-xl p-2 hover:bg-slate-100"><X/></button></div><div className="mt-6 overflow-hidden rounded-2xl border border-slate-100"><div className="grid grid-cols-[1fr_auto] bg-slate-950 px-4 py-3 text-xs font-bold uppercase tracking-wider text-white"><span>Outlet</span><span>Live price</span></div>{entries.map(([name,price],i)=><div key={name} className="grid grid-cols-[1fr_auto] items-center border-t px-4 py-4"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-lg bg-slate-100"><Store className="h-4 w-4 text-slate-500"/></div><div><div className="text-sm font-black">{name}</div>{i===0?<Badge tone="green" className="mt-1">Lowest in network</Badge>:<div className="mt-1 text-xs text-slate-400">{money(price-entries[0][1])} above lowest</div>}</div></div><div className="text-right"><div className="text-lg font-black">{money(price)}</div>{i>0&&<div className="text-[10px] text-slate-400 line-through">{money(price*1.04)}</div>}</div></div>)}</div><div className="mt-6 rounded-2xl bg-emerald-50 p-5"><div className="flex items-start gap-3"><ArrowDownRight className="mt-0.5 h-5 w-5 text-emerald-700"/><div><div className="font-black text-emerald-900">Price match insight</div><div className="mt-1 text-sm text-emerald-800">QueueZero routes the comparison around the store context you selected so your in-store decision is faster than checking competitor sites manually.</div></div></div></div><Button className="mt-5 w-full" onClick={onClose}>Done</Button></div></div>}
