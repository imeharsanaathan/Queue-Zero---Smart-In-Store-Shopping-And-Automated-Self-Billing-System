'use client';
import {useState} from 'react'; import {MapPin,ShieldCheck} from 'lucide-react'; import {useRouter} from 'next/navigation'; import {BrandMark} from '@/components/brand-mark'; import type {City} from '@/lib/data';
export function CitySelector({cities}:{cities:City[]}){const router=useRouter();const[busy,setBusy]=useState(false);async function selectCity(cityId:string){if(busy)return;setBusy(true);try{const res=await fetch('/api/context',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({cityId})});if(res.ok)router.push('/store');else setBusy(false)}catch{setBusy(false)}}return <div className="mx-auto max-w-6xl pt-8 sm:pt-14"><div className="mb-8 flex justify-center"><BrandMark/></div><div className="overflow-hidden rounded-[32px] border border-emerald-100 bg-white p-6 shadow-[0_20px_70px_rgba(16,185,129,.14)] sm:p-10"><div className="rounded-3xl bg-gradient-to-r from-emerald-50 via-white to-blue-50 p-6 text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-gradient-to-br from-emerald-400 to-cyan-400 text-white shadow-lg"><MapPin className="h-7 w-7"/></div><p className="mt-4 text-xs font-black uppercase tracking-[.2em] text-emerald-700">Step 1 of 3</p><h1 className="mt-2 text-3xl font-black sm:text-5xl">Choose your city</h1><p className="mx-auto mt-3 max-w-2xl text-sm text-slate-500">Select your shopping city. QueueZero will continue only after you click a city.</p></div><div className="mt-8 grid grid-cols-3 gap-2 sm:grid-cols-2 sm:gap-3 lg:grid-cols-4">{cities.map(c=><button key={c.id} type="button" disabled={busy} onClick={()=>selectCity(c.id)} className="min-h-[160px] min-w-0 overflow-hidden rounded-2xl border border-slate-200 bg-white p-2.5 text-left transition hover:-translate-y-1 hover:border-blue-200 hover:shadow-xl disabled:cursor-wait disabled:opacity-70 sm:min-h-[210px]"><div className="flex h-[88px] items-center justify-center overflow-hidden rounded-xl bg-slate-50 sm:h-[122px]">{c.image?<img src={c.image} alt={`${c.name} landmark`} loading="lazy" decoding="async" className="h-full w-full object-contain p-1"/>:<MapPin className="h-8 w-8 text-slate-300"/>}</div><div className="mt-2 flex items-center justify-between gap-2"><div className="text-xs font-black uppercase tracking-wider text-slate-400">{c.short}</div><div className="text-[9px] font-bold text-emerald-600">Select</div></div><div className="mt-1 whitespace-normal break-normal text-[12px] font-black leading-4 sm:text-lg sm:leading-normal">{c.name}</div></button>)}</div><div className="mt-8 flex items-center gap-2 border-t border-slate-100 pt-6 text-xs font-bold text-slate-500"><ShieldCheck className="h-4 w-4 shrink-0 text-emerald-600"/>Click a city to continue. No city is entered until you choose one.</div></div></div>}
