'use client';
import {useState} from 'react'; import {MapPin,ArrowRight,ShieldCheck} from 'lucide-react'; import {useRouter} from 'next/navigation'; import {BrandMark} from '@/components/brand-mark'; import {Button} from '@/components/ui/button'; import type {City} from '@/lib/data';
export function CitySelector({cities}:{cities:City[]}){const router=useRouter();const[selected,setSelected]=useState('');const[busy,setBusy]=useState(false);async function next(){if(!selected)return;setBusy(true);const res=await fetch('/api/context',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({cityId:selected})});if(res.ok)router.push('/store');else setBusy(false)}return <div className="mx-auto max-w-6xl pt-8 sm:pt-14"><div className="mb-8 flex justify-center"><BrandMark/></div><div className="overflow-hidden rounded-[32px] border border-emerald-100 bg-white p-6 shadow-[0_20px_70px_rgba(16,185,129,.14)] sm:p-10"><div className="rounded-3xl bg-gradient-to-r from-emerald-50 via-white to-blue-50 p-6 text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-gradient-to-br from-emerald-400 to-cyan-400 text-white shadow-lg"><MapPin className="h-7 w-7"/></div><p className="mt-4 text-xs font-black uppercase tracking-[.2em] text-emerald-700">Step 1 of 3</p><h1 className="mt-2 text-3xl font-black sm:text-5xl">Choose your city</h1><p className="mx-auto mt-3 max-w-2xl text-sm text-slate-500">Select your shopping city. QueueZero will not continue until you choose one.</p></div><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{cities.map(c=><button key={c.id} onClick={()=>setSelected(c.id)} className={`rounded-2xl border p-5 text-left transition hover:-translate-y-1 hover:shadow-xl ${selected===c.id?'border-emerald-500 bg-emerald-50 ring-2 ring-emerald-200':'border-slate-200 bg-white hover:border-blue-200'}`}><div className="text-xs font-black uppercase tracking-wider text-slate-400">{c.short}</div><div className="mt-2 text-lg font-black">{c.name}</div></button>)}</div><div className="mt-8 flex flex-col items-center justify-between gap-4 border-t border-slate-100 pt-6 sm:flex-row"><div className="flex items-center gap-2 text-xs font-bold text-slate-500"><ShieldCheck className="h-4 w-4 text-emerald-600"/>City selection is required</div><Button size="lg" variant="success" disabled={!selected||busy} onClick={next}>Continue to store selection <ArrowRight className="h-5 w-5"/></Button></div></div></div>}
