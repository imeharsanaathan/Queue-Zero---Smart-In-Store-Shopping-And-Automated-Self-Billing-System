'use client';
import {useMemo,useState} from 'react'; import {MapPin,ArrowRight,Store as StoreIcon,ShieldCheck,ChevronLeft,Search,Building2} from 'lucide-react'; import {useRouter} from 'next/navigation'; import {BrandMark} from '@/components/brand-mark'; import {Button} from '@/components/ui/button'; import type {City,Store} from '@/lib/data';
export function StoreSelector({city,stores}:{city:City;stores:Store[]}){const router=useRouter();const[selected,setSelected]=useState('');const[busy,setBusy]=useState(false);const[q,setQ]=useState('');const filtered=useMemo(()=>{const needle=q.trim().toLowerCase();if(!needle)return stores;return stores.filter(s=>`${s.name} ${s.brand} ${s.address} ${s.kind}`.toLowerCase().includes(needle))},[q,stores]);async function next(){if(!selected)return;setBusy(true);const res=await fetch('/api/context',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({cityId:city.id,storeId:selected})});if(res.ok)router.push('/');else setBusy(false)}return <div className="mx-auto max-w-6xl pt-8 sm:pt-14"><div className="mb-8 flex justify-center"><BrandMark/><div className="hidden"/></div><div className="overflow-hidden rounded-[32px] border border-blue-100 bg-white p-6 shadow-[0_20px_70px_rgba(37,99,235,.12)] sm:p-10"><div className="rounded-3xl bg-gradient-to-r from-blue-50 via-white to-cyan-50 p-6"><div className="flex flex-col gap-4 border-b border-blue-100 pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-black uppercase tracking-[.2em] text-blue-700">Step 2 of 3</p><h1 className="mt-2 text-3xl font-black sm:text-5xl">Choose your store</h1><p className="mt-3 text-sm text-slate-500">Select exactly one outlet in <b>{city.name}</b> before opening the shopping catalog.</p></div><button onClick={()=>router.push('/city')} className="flex items-center gap-1 self-start text-xs font-black text-blue-700"><ChevronLeft className="h-4 w-4"/>Change city</button></div><div className="mt-5 relative"><Search className="absolute left-4 top-1/2 h-5 w-5 -translate-y-1/2 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search store by name, brand, area or category..." className="h-12 w-full rounded-2xl border border-slate-200 bg-white pl-12 pr-4 text-sm font-semibold outline-none focus:border-blue-400 focus:ring-4 focus:ring-blue-500/10"/></div></div><div className="mt-8 mb-4 flex items-center justify-between"><div className="text-sm font-bold text-slate-500">{filtered.length} stores found</div>{q&&<button onClick={()=>setQ('')} className="text-xs font-black text-blue-600">Clear search</button>}</div><div className="grid gap-4 md:grid-cols-2">{filtered.map(s=><button key={s.id} onClick={()=>setSelected(s.id)} className={`rounded-2xl border p-5 text-left transition hover:-translate-y-1 hover:shadow-xl ${selected===s.id?'border-blue-500 bg-blue-50 ring-2 ring-blue-200':'border-slate-200 bg-white hover:border-blue-200'}`}><div className="flex items-start justify-between gap-3"><div className="rounded-xl bg-gradient-to-r from-emerald-500 via-cyan-500 to-blue-600 px-3 py-2 text-xs font-black text-white">{s.brand}</div><span className="rounded-full bg-emerald-100 px-2.5 py-1 text-[10px] font-black uppercase text-emerald-700">{s.kind}</span></div><div className="mt-4 text-lg font-black">{s.name}</div><div className="mt-2 flex gap-2 text-xs text-slate-500"><MapPin className="h-4 w-4 shrink-0"/><span>{s.address}</span></div><div className="mt-4 flex items-center gap-2 text-xs font-bold text-slate-500"><Building2 className="h-4 w-4 text-blue-600"/>Store-specific catalog</div></button>)}</div>{filtered.length===0&&<div className="rounded-3xl border border-dashed border-slate-300 bg-slate-50 p-10 text-center"><StoreIcon className="mx-auto h-10 w-10 text-slate-300"/><div className="mt-3 text-lg font-black">No matching store</div><div className="mt-1 text-sm text-slate-500">Try the store name, brand, locality or store type.</div></div>}<div className="mt-8 flex flex-col items-center justify-between gap-4 border-t border-slate-100 pt-6 sm:flex-row"><div className="flex items-center gap-2 text-xs font-bold text-slate-500"><ShieldCheck className="h-4 w-4 text-emerald-600"/>Only the selected outlet's products will be shown</div><Button size="lg" variant="success" disabled={!selected||busy} onClick={next}>Enter store <ArrowRight className="h-5 w-5"/></Button></div></div></div>}
