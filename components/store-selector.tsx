'use client';
import {useMemo,useState} from 'react'; import {MapPin,Store as StoreIcon,ShieldCheck,ChevronLeft,Search,Building2} from 'lucide-react'; import {useRouter} from 'next/navigation'; import {BrandMark} from '@/components/brand-mark'; import type {City,Store} from '@/lib/data';
export function StoreSelector({city,stores}:{city:City;stores:Store[]}){const router=useRouter();const[busy,setBusy]=useState(false);const[q,setQ]=useState('');const filtered=useMemo(()=>{const needle=q.trim().toLowerCase();if(!needle)return stores;return stores.filter(s=>`${s.name} ${s.brand} ${s.address} ${s.kind}`.toLowerCase().includes(needle))},[q,stores]);async function selectStore(storeId:string){if(busy)return;setBusy(true);try{const res=await fetch('/api/context',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({cityId:city.id,storeId})});if(res.ok)router.push('/');else setBusy(false)}catch{setBusy(false)}}return <div className="mx-auto max-w-6xl pt-8 sm:pt-14"><div className="mb-8 flex justify-center"><BrandMark/></div><div className="overflow-hidden rounded-[32px] border border-blue-100 bg-white p-6 shadow-[0_20px_70px_rgba(37,99,235,.12)] sm:p-10"><div className="rounded-3xl bg-gradient-to-r from-blue-50 via-white to-cyan-50 p-6"><div className="flex flex-col gap-4 border-b border-blue-100 pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-black uppercase tracking-[.2em] text-blue-700">Step 2 of 3</p><h1 className="mt-2 text-3xl font-black sm:text-5xl">Choose your store</h1><p className="mt-3 text-sm text-slate-500">Select exactly one outlet in <b>{city.name}</b> before opening the shopping catalog.</p></div><button onClick={()=>router.push('/city')} className="flex items-center gap-1 self-start text-xs font-black text-blue-700"><ChevronLeft className="h-4 w-4"/>Change city</button></div><div className="mt-5 relative"><Search className="absolute left-4 top-1/2 h-5 w-5 -translate-y-1/2 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search store by name, brand, area or category..." className="h-12 w-full rounded-2xl border border-slate-200 bg-white pl-12 pr-4 text-sm font-semibold outline-none focus:border-blue-400 focus:ring-4 focus:ring-blue-500/10"/></div></div><div className="mt-8 mb-4 flex items-center justify-between"><div className="text-sm font-bold text-slate-500">{filtered.length} stores found</div>{q&&<button onClick={()=>setQ('')} className="text-xs font-black text-blue-600">Clear search</button>}</div><div className="grid grid-cols-3 gap-2 sm:gap-4 md:grid-cols-2">{filtered.map(s=><button key={s.id} type="button" disabled={busy} onClick={()=>selectStore(s.id)} className="flex min-h-[220px] min-w-0 flex-col overflow-hidden rounded-2xl border border-slate-200 bg-white p-2.5 text-left transition hover:-translate-y-1 hover:border-blue-200 hover:shadow-xl disabled:cursor-wait disabled:opacity-70 sm:min-h-[260px]"><div className="flex h-[90px] items-center justify-center overflow-hidden rounded-xl bg-slate-50 sm:h-[112px]">{s.logoKey?<img src={s.logoKey} alt={`${s.brand} logo`} loading="lazy" decoding="async" className="h-full w-full object-contain p-3"/>:<div className="px-2 text-center text-sm font-black text-slate-300">{s.brand}</div>}</div><div className="mt-2 flex min-w-0 items-center justify-between gap-2"><div className="max-w-[60%] truncate text-[10px] font-black uppercase text-slate-900 sm:text-xs">{s.brand}</div><span className="max-w-[40%] truncate rounded-full bg-emerald-100 px-2 py-1 text-[8px] font-black uppercase text-emerald-700 sm:text-[10px]">{s.kind}</span></div><div className="mt-2 min-h-[32px] min-w-0 whitespace-normal break-words text-[12px] font-black leading-4 sm:mt-3 sm:min-h-0 sm:text-lg sm:leading-normal">{s.name}</div><div className="mt-2 hidden gap-2 text-xs text-slate-500 sm:flex"><MapPin className="h-4 w-4 shrink-0"/><span>{s.address}</span></div><div className="mt-3 hidden items-center gap-2 text-xs font-bold text-slate-500 sm:mt-4 sm:flex"><Building2 className="h-4 w-4 text-blue-600"/>Store-specific catalog</div><div className="mt-auto pt-3 whitespace-nowrap text-[10px] font-black text-blue-600 sm:text-xs">Click to enter store</div></button>)}</div>{filtered.length===0&&<div className="rounded-3xl border border-dashed border-slate-300 bg-slate-50 p-10 text-center"><StoreIcon className="mx-auto h-10 w-10 text-slate-300"/><div className="mt-3 text-lg font-black">No matching store</div><div className="mt-1 text-sm text-slate-500">Try the store name, brand, locality or store type.</div></div>}<div className="mt-8 flex items-center gap-2 border-t border-slate-100 pt-6 text-xs font-bold text-slate-500"><ShieldCheck className="h-4 w-4 shrink-0 text-emerald-600"/>Click a store to continue. No store is entered until you choose one.</div></div></div>}
